{
  "schema": 1,
  "description": "Extracts information from knowledge base.",
  "execution_settings": {
    "default": {
      "max_tokens": 500,
      "temperature": 0.3
    }
  },
  "input_variables": [
    {
      "name": "input",
      "description": "The raw text or request from the user containing meeting details.",
      "required": true
    },
    {
      "name": "collection",
      "description": "The knowledge base collection to query.",
      "required": true
    }
  ]
}