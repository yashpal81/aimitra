{
  "schema": 1,
  "description": "Instruction on the display format for Data generated from LLM",
  "execution_settings": {
    "default": {
      "max_tokens": 500,
      "temperature": 0.3
    }
  },
  "input_variables": [
    {
      "name": "input",
      "description": "The raw text or request from the user.",
      "required": true
    },
    {
      "name": "currentDateTime",
      "description": "The current date and time to anchor relative dates like 'tomorrow' or 'next Monday'.",
      "required": false
    }
  ]
}